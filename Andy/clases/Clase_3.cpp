#include <iostream>
using namespace std;

int main()
{
    //Ciclo de repeticion definida
    /*
    string nombre;
    unsigned nota;
    
    for(int i=1; i<=5; i=i+1)
    {
      cout << "Ingrese el nombre ";
      cin >> nombre;
      cout << "Ingrese la nota ";
      cin >> nota;  
      if(nota>=6)
        cout << nombre << " APROBADO"<<endl;
      else   
        cout << nombre << " DESAPROBADO"<<endl;
    }
    */

    //Contador y acumulador
    //Ingresar 5 numeros enteros, informar la cantidad de numeros negativos
    //y la sumatoria de los positivos.
    /*
    int cont=0, suma=0, num, i;

    for(i=1; i<=5; i++)
    {
      cout << "Ingrese el numero de orden " << i << endl;
      cin >> num;
      if(num<0)
        //cont = cont + 1;
        //cont += 1;
        cont++; //++cont;
      else
        //suma = suma + num;
        suma+=num;
    }
    
    cout << "Cantidad de negativos = " << cont << endl;
    cout << "Suma de positivos = " << suma;
    */
    
    //Mostrar los numeros pares entre 30 y 10 de manera descendente
    //for(int i=30; i>=10; i-=2)
    //    cout << i << " ";

    //Ciclo de repeticion definida
    /*
    string nombre;
    unsigned nota, cant;
    cout << "Ingrese la cantidad de alummnos del curso: "<<endl;
    cin >> cant;
    
    for(int i=1; i<=cant; i=i+1)
    {
      cout << "Ingrese el nombre "<<endl;
      cin >> nombre;
      cout << "Ingrese la nota "<<endl;
      cin >> nota;  
      if(nota>=6)
        cout << nombre << " APROBADO"<<endl;
      else   
        cout << nombre << " DESAPROBADO"<<endl;
    }
    */
    return 0;


}