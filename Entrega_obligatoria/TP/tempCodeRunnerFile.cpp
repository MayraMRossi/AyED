    // Arch_rep r;
    // fread(&r, sizeof(Arch_rep), 1, f);
    // while(!feof(f) && r.zona != zona)
    // {
    //     fread(&r, sizeof(Arch_rep), 1, f);
    // }
    // if(feof(f))
    //     return false;
    // else